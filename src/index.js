import React from 'react';
import ReactDOM from 'react-dom/client';
import CarbonDashboard from './CarbonDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<CarbonDashboard />);
